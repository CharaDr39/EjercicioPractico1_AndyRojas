spring.application.name=EjercicioPractico_AndyRojas

# Configuración de la conexión a MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/farmacia?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=141017chara

# Driver de MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Dialecto de Hibernate para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configurar el servidor para que escuche en el puerto 78
server.port=78

# Configurar los logs a nivel TRACE
logging.level.root=TRACE

# Formato del log con fecha, hora y minuto
logging.pattern.console=%d{dd-MM-yyyy HH:mm:ss} - %msg%n

# Ubicación correcta de plantillas para evitar errores de rutas
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
